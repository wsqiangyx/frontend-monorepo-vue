// ============================================================================
// prettier.config.js — Prettier 格式化规则
// ============================================================================
// 定义团队统一的代码格式规范。ESLint 负责代码质量，Prettier 负责代码格式，
// 两者分工明确：eslint-config-prettier 会关闭与 Prettier 冲突的 ESLint 规则。
// ============================================================================
export default {
  // 不使用分号。团队风格约定，减少视觉噪音。
  semi: false,

  // 使用单引号。JS/TS 社区主流风格，与双引号无功能差异。
  singleQuote: true,

  // 尾随逗号。使 Git diff 更干净——新增项只需改一行，不影响前一行。
  trailingComma: 'all',

  // 单行最大宽度 100 字符。兼顾可读性和屏幕利用率。
  printWidth: 100,

  // 缩进 2 个空格。与 .editorconfig 保持一致。
  tabWidth: 2,

  // 强制 LF 换行符。跨平台协作必须统一，避免 Git 显示全文件变更。
  endOfLine: 'lf',

  // 箭头函数参数始终加括号。保持一致性，避免单参数时格式来回变化。
  arrowParens: 'always',
}
