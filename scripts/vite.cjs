// ============================================================================
// vite.cjs — Vite 启动包装脚本
// ============================================================================
// 统一通过包装脚本启动 Vite，保证各 workspace 行为一致。
// 所有 workspace 的 package.json 中 dev/build 脚本调用
// node ../../scripts/vite.cjs 而非直接调用 vite，
// 确保 Windows 兼容性和 bin 路径解析逻辑统一。
// ============================================================================
const { run } = require('./run-vite-bin.cjs')

run('vite')
