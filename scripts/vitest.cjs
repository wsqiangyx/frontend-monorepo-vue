// ============================================================================
// vitest.cjs — Vitest 启动包装脚本
// ============================================================================
// 统一通过包装脚本启动 Vitest，保证各 workspace 行为一致。
// 所有 workspace 的 package.json 中 test 脚本调用
// node ../../scripts/vitest.cjs 而非直接调用 vitest，
// 确保 Windows 兼容性和 bin 路径解析逻辑统一。
// ============================================================================
const { run } = require('./run-vite-bin.cjs')

run('vitest')
