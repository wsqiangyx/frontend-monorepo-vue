// ============================================================================
// @repo/shared — ID 生成工具
// ============================================================================

/**
 * 生成唯一标识符，基于 crypto.randomUUID()。
 * 返回 UUID v4 格式，122 位随机性，不可猜测。
 */
export function generateId(): string {
  return crypto.randomUUID()
}
