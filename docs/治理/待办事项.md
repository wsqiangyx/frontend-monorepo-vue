# 治理待办事项

> 制定日期：2026-05-17
> 文档性质：阶段性治理待办与后续改进清单

## 可选完善项

- 为文档导航与引用增加 `docs:check-links` 自动校验脚本，并按需接入 CI
- 在详细设计模板中补充“上游设计 / 前置依赖 / 派生自查”清单
- 增加 `check:arch`，至少先校验 `platform-core` 不得依赖 React / DOM / UI 库
- 增加 `docs:check`，校验根文档与总体设计主文档的关键术语和架构口径一致
- 为 `packages/mock` 的正式使用边界增加自动化检查，防止宿主测试绕过 MSW 正式入口
